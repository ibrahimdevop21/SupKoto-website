---
import LatestWorkCarouselComponent from './LatestWorkCarousel';
import VisibleWrapper from './VisibleWrapper';

// Use Astro's built-in i18n to determine language direction
const rtl = Astro.currentLocale === 'ar';
---

<div class="opacity-100" style="opacity: 1;">
  <LatestWorkCarouselComponent isArabic={rtl} client:only="react" />
</div>
