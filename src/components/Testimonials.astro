---
import TestimonialsReact from './Testimonials';
import { isRTL } from '../i18n/utils';

const rtl = isRTL(Astro);
---

<div class="opacity-100" style="opacity: 1;">
  <TestimonialsReact isArabic={rtl} client:only="react" />
</div>
