---
import HeaderReact from './HeaderReact';
import { getCurrentLocale, isRTL } from '../i18n/utils';

const lang = getCurrentLocale(Astro);
const rtl = isRTL(Astro);
---

<HeaderReact client:load currentLocale={lang} isRTL={rtl} />
