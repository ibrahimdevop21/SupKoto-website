// Basic TypeScript definitions for the project

// Example of a simple interface
export interface User {
  id: string;
  name: string;
  email?: string;
}

// Example of a simple type
export type Theme = 'light' | 'dark' | 'system';

// This file ensures TypeScript has at least one .ts file to process
